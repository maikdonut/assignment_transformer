[project]
name = "assignment-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "scikit-learn>=1.6.0",
    "torch>=2.13.0",
    "transformers>=5.15.0",
]
